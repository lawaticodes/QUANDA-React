import React from 'react';

class Homepage extends React.Component {
  render() {
    return(
      <p>This is what the homepage looks like when you have logged in.</p>
    );
  }
}

export default Homepage;
